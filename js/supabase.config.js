/* =============================================================
   Ótica Redentora — Configuração do Supabase
   Preencha com os dados do seu projeto (Supabase → Settings → API).
   A chave "anon / publishable" é PÚBLICA por design (protegida por RLS)
   — pode ser commitada sem problema.
   ============================================================= */
window.SUPABASE_CONFIG = {
  url:     '',   // ex.: https://xxxxxxxxxxxx.supabase.co
  anonKey: ''    // chave "anon public" (Project API keys)
};
